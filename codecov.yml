comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  require_changes: false
coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

fixes:
  - "/workspace/::"
